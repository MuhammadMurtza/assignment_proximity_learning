<%= turbo_frame_tag :modal do %>
  <div class="modal fade" id="new-data-modal" tabindex="-1" aria-labelledby="newItemRemoteModalLabel" aria-hidden="true" data-controller="show-modal" data-action="turbo:before-render@document->show-modal#hideBeforeRender">
    <div class="modal-dialog" role="document">
      <div id="replaced">
        <%= render "form", user: @user %>
      </div>
    </div>
  </div>
<% end %>
